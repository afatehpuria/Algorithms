#include <iostream>
#include "include/sorting.h"

using namespace std;

int main() {
    ///////////////////////////////////////////////////////////////////////////
    /// list is a reserved name in the std namespace; changed to unsortedList
    ///////////////////////////////////////////////////////////////////////////
    /// int list[] = {64, 34, 25, 12, 22, 11, 90};
    ///////////////////////////////////////////////////////////////////////////
    int unsortedList[] = {64, 34, 25, 12, 22, 11, 90};    
    ///////////////////////////////////////////////////////////////////////////
    /// You need to instantiate a variable of type sorting before calling a
    /// method
    ///////////////////////////////////////////////////////////////////////////
    /// sorting::sorting(list);
    ///////////////////////////////////////////////////////////////////////////
    sorting s(unsortedList, sizeof(unsortedList)/sizeof(unsortedList[0x0]));
    ///////////////////////////////////////////////////////////////////////////
    // std::cout << "Hello, World!" << std::endl;
    ///////////////////////////////////////////////////////////////////////////
    s.BubbleSort();
    ///////////////////////////////////////////////////////////////////////////
    s.PrintArray();
    ///////////////////////////////////////////////////////////////////////////
    return 0;
}
